%h1 MyDnD
%h2= "Admin: #{@username}"
%h3 Reported sessions
-@reportedsessions.each do |session|
  %h3= link_to "Id session: #{session.id}", session_path(session.id)
  %h3= link_to "Session: #{session.title}", session_path(session.id)

%td= button_to "Home", home_path, :method => :get
